export type OrderType = {
      userId: string;
      items: [];
      address: string;
      appartment: string;
      city: string;
      postalCode: string;
      totalPrice: number;
      status: string;
      orderDate: string;
}